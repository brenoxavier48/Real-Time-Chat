import React from 'react';
import './styles.css';
import ChatContainer from '../../components/ChatContainer'

const Chat = (props) => {
    return(
        <div className="chat-page">
            <ChatContainer>
                kole
            </ChatContainer>
        </div>
    )
}

export default Chat